version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - python3 -m venv /tmp/authority-delta-deploy-env
      - /tmp/authority-delta-deploy-env/bin/python -m pip install --no-index --find-links vendor/wheels --require-hashes -r requirements-deploy.lock
  build:
    commands:
      - PYTHONPATH=src /tmp/authority-delta-deploy-env/bin/python scripts/deploy_baseline.py
